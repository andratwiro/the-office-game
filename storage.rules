rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Uploaded GIFs for trivia questions. Signed-in (anonymous is fine) only.
    match /gifs/{file=**} {
      allow read, write: if request.auth != null;
    }
  }
}
